export enum UserRole {
  RENTER = 'renter',
  OWNER = 'owner',
  SHOP = 'shop',
  AGENT = 'agent',
  ADMIN = 'admin',
}

export enum UserStatus {
  ACTIVE = 'active',
  LIMITED = 'limited',
  BANNED = 'banned',
}

export enum TrustTier {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum Language {
  EN = 'en',
  HI = 'hi',
}
